function multiplyBy11(input) {return ["0", "0"].concat(input.split('')).concat(["0"]).map(a => a.charCodeAt(0) - 48).map((a, index, arr) => a + arr[index + 1]).slice(0, -1).concat([0]).reduceRight((accumulator, currentValue) => ({arr: accumulator.arr.concat((currentValue + accumulator.holder) % 10), holder: Math.floor((currentValue + accumulator.holder)/10)}), {arr: [], holder: 0}).arr.reverse().slice(0, -1).join('').replace(/^0+/, "");}
